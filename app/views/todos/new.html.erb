<h1>New todo entry</h1>
 
 
<%= form_with scope: :todo, url: todos_path, local: true do |entry| %>
  <p>
    <%= entry.text_area :text,  :placeholder => 'Название задачи...'%>
  </p>
    <%#= entry.collection_select :project_id, Project.order(:title),:id,:title, include_blank: false %>
    <%= entry.select :project_id, Project.all.pluck(:title, :id), {class: "form-control"}%>
    
  <p>
    <%#= entry.submit %>
    <%#= link_to 'OK', "/", :onclick => entry.submit() %>
  </p>
<% end %>
<%= link_to 'Отмена', projects_index_path %>
